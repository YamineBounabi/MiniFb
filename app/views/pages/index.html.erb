<% @walls.each do |wall| %>
<div class="row">
  <span class="small-6 columns"><%= link_to wall.user.name, "/pages/#{wall.user.id}", class:"button tiny" %></span>
  <span class="small-6 columns"><%= link_to wall.relative.name, "/pages/#{wall.relative.id}", class:"button tiny" %></span>
</div>
<div class="row">
  <div class="large-12 columns wall-body">
    <span><%= wall.body %></span>
  </div>
</div>
</div>
<% end %>
